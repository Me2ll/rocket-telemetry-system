import React from "react";
import logo from "../assets/logo/503.png";

const UnavailablePage = (props) => {
  return (
    <img
      src={logo}
      style={{
        width: "70%",
        height: "70%",
        marginBottom: "10%",
        marginLeft: "15%",
      }}
    />
  );
};

export default UnavailablePage;
